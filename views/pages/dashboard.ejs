<!DOCTYPE html>
<html lang="en">
<head>
  <title>user details</title>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
</head> 


<body>
 <!-- <% if(typeof locals.session == 'undefined' || typeof locals.session.user_id == 'undefined'){ %>
            <a id='home' class="nav-link" href="/">Home</a>
            <a id='links' class="nav-link" href="/register">Register</a>
            <a id='list' class="nav-link" href="/login">Login</a>
        <% }else{%>
             <a id='home' class="nav-link" href="/">Home</a>
             <a id='home' class="nav-link" href="/">dashboard</a>
            <a id='list' class="nav-link" href="/logout">Logout</a>
        <% } %> -->
<div class="container">
 
  
  <table class="table"   >
    <thead>
      <tr>
        <th>Name</th>
        <th>mobile</th>
        <th>Email</th>
      </tr>
    </thead>
    <tbody>

          

      <% 
      result.forEach(function(result,i){ %>
        <tr> 
           <td><%- result.name %></td> 
           <td><%- result.mobile %></td>
           <td><%- result.email %></td>
         
          
          </tr>
          <% } ) %>

          <a href='http://localhost:5000'><button type="button" class="btn btn-success">Log Out</button></a>

    </tbody>
  </table>

</div>

</body>
</html>
